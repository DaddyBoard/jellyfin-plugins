name: Official test disabled

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Official jellyfin-meta-plugins tests are disabled. CI builds the plugin instead."
